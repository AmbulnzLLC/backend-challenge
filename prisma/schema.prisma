// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// pizza to be added to order 
model Pizza {
  id       Int      @id @default(autoincrement())
  Orders  Orderitem[]
  flavor   Flavor @default(CHEESE)
  //TODO: we will add functionality for customers to create accounts later
  // customer    User     @relation(fields: [authorId], references: [id])
  // customerId  Int
}

enum Flavor {
  CHEESE
  PEPPERONI
  VEGGIE
  MEAT
}

// keeps track of the order and when it was made/updated
model Order {
  id     Int     @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  items  Orderitem[]
} 

//TODO: How do we keep track of the quantity?
// explicitly connects order to the amount of pizzas contained in the them 
 model Orderitem {

  pizza  Pizza   @relation(fields: [pizzaId], references: [id])
  pizzaId Int 
  order Order  @relation(fields: [orderId], references: [id])
  orderId Int     @unique
  quantity Int

  @@id([pizzaId, orderId])
}

        








//TODO: User Model to be added later
// model User {
//   id      Int      @id @default(autoincrement())
//   email   String   @unique
//   name    String?
//   posts   Post[]
//   profile Profile?
// }